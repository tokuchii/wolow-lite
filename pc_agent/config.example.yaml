# WOLOW Lite PC Agent Configuration
# Copy to config.yaml and edit

server:
  host: "0.0.0.0"
  port: 8220
  # Generate a token: python -c "import secrets; print(secrets.token_urlsafe(32))"
  token: "CHANGE_ME_TO_A_RANDOM_TOKEN"

logging:
  level: "INFO"
